# RuvVector Service Configuration
# Copy this file to .env for local development

# =============================================================================
# Service Configuration
# =============================================================================

# Service port
RUVVECTOR_SERVICE_PORT=3100

# =============================================================================
# Database Connection
# =============================================================================

# Method 1: Use DATABASE_URL (recommended)
# For local development (host machine):
DATABASE_URL=postgresql://ruvector:ruvector_secret@localhost:5432/ruvector

# For Docker development (use with docker-compose):
# DATABASE_URL=postgresql://ruvector:ruvector_secret@ruvector-postgres:5432/ruvector

# Method 2: Use individual variables (alternative)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=ruvector
POSTGRES_PASSWORD=ruvector_secret
POSTGRES_DB=ruvector
